version: '3.8'


services:
  selb-app:
    image: selb-app
    container_name: selb-app-container
    ports:
      - "3000:3000"

  nginx:
    image: nginx
    container_name: nginx-container
    volumes: 
    - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    ports:
      - "80:80"



